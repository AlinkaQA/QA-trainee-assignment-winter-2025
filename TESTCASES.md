## 1. Создание объявления (`POST /api/1/item`)

### 1.1 Позитивные тесты

#### ТК-01 Успешное создание объявления  
**Предусловия:**  
- Уникальный sellerID в диапазоне 111111-999999  

**Шаги:**  
1. Отправить `POST` запрос с валидными данными объявления  
2. Проверить, что статус ответа `200 OK`  
3. Проверить, что в ответе есть `itemID`  

**Тело запроса:**
```json
{
  "sellerID": 112277,
  "name": "Example Item",
  "price": 1000,
  "description": "Description of the item"
}
```

**Ожидаемый результат:**  
Сервер возвращает status: "Сохранили объявление - {itemID}"  
```json
{
    "status": "Сохранили объявление - 271a2549-611b-4ca4-abf3-eb0bc8838836"
}
```

---

### 1.2 Негативные тесты  

#### ТК-02 Создание объявления без sellerID  
**Шаги:**  
1. Отправить `POST` запрос без `sellerID`  
2. Проверить, что статус ответа `400 Bad Request`  
3. Проверить сообщение об ошибке  

**Тело запроса:**
```json
{
  "name": "Example Item",
  "price": 1000,
  "description": "Description of the item"
}
```

**Ожидаемый результат:**  
Сервер возвращает 400 Bad Request, сообщение "sellerID is required" 

---

## 2. Получение объявления по itemID (`GET /api/1/item/{itemID}`)

#### ТК-03 Получение существующего объявления  
**Шаги:**  
1. Отправить `GET` запрос с существующим `itemID`  
2. Проверить, что статус ответа `200 OK`  
3. Проверить, что данные соответствуют ранее созданному объявлению

**Запрос:**
GET /api/1/item/{itemID}  (https://qa-internship.avito.com/api/1/item/271a2549-611b-4ca4-abf3-eb0bc8838836)

**Ожидаемый результат:**  
```json
[
    {
        "createdAt": "2025-02-15 17:13:52.412149 +0300 +0300",
        "id": "271a2549-611b-4ca4-abf3-eb0bc8838836",
        "name": "dsdsd",
        "price": 1000,
        "sellerId": 112277,
        "statistics": {
            "contacts": 0,
            "likes": 0,
            "viewCount": 0
        }
    }
]
```
---

#### ТК-04 Получение несуществующего объявления  
**Шаги:**  
1. Отправить `GET` запрос с `itemID`, которого нет в системе  
2. Проверить, что статус ответа `404 Not Found`  
3. Проверить сообщение об ошибке

**Запрос:**  
GET /api/1/item/{itemID}  (https://qa-internship.avito.com/api/1/item/cdba6dea-d852-432a-b46e-ad1a2ac20ac9)

**Ожидаемый результат:**  
- Сервер возвращает `404 Not Found`, сообщение "Item not found"  

```json
{
    "result": {
        "message": "item cdba6dea-d852-432a-b46e-ad1a2ac20ac9 not found",
        "messages": null
    },
    "status": "404"
}
```
---

## 3. Получение всех объявлений по sellerID (`GET /api/1/{sellerID}/item`)

#### ТК-05 Получение объявлений существующего продавца  
**Шаги:**  
1. Отправить `GET` запрос с `sellerID`, у которого есть объявления  
2. Проверить, что статус ответа `200 OK`  
3. Проверить, что в ответе есть список объявлений  

**Запрос:** 
GET /api/1/{sellerID}/item  (https://qa-internship.avito.com/api/1/112277/item)

**Ожидаемый результат:**  
- Возвращается список объявлений 

```json
[
    {
        "createdAt": "2025-02-15 17:13:52.412149 +0300 +0300",
        "id": "dsdsd",
        "name": "271a2549-611b-4ca4-abf3-eb0bc8838836",
        "price": 1000,
        "sellerId": 112277,
        "statistics": {
            "contacts": 0,
            "likes": 0,
            "viewCount": 0
        }
    }
]
```
---

#### ТК-06 Получение объявлений для sellerID, у которого нет объявлений  
**Шаги:**  
1. Отправить `GET` запрос с `sellerID`, у которого нет объявлений  
2. Проверить, что статус ответа `200 OK`  
3. Проверить, что список пуст

**Запрос:**
GET /api/1/{sellerID}/item (https://qa-internship.avito.com/api/1/116677/item)

**Ожидаемый результат:**  
- Возвращается пустой список 

```json
 []
```

---

## 4. Получение статистики (`GET /api/1/statistic/{itemID}`)

#### ТК-07 Получение статистики существующего объявления  
**Шаги:**  
1. Отправить `GET` запрос с `itemID`, у которого есть статистика  
2. Проверить, что статус ответа `200 OK`  
3. Проверить, что в ответе есть данные статистики
 
**Запрос:**  
https://qa-internship.avito.com/api/1/statistic/271a2549-611b-4ca4-abf3-eb0bc8838836
**Ожидаемый результат:**
- Возвращается корректная статистика   

```json
 [
    {
        "contacts": 0,
        "likes": 0,
        "viewCount": 0
    }
]
```

---

#### ТК-08 Получение статистики для несуществующего объявления  
**Шаги:**  
1. Отправить `GET` запрос с `itemID`, которого нет в системе  
2. Проверить, что статус ответа `404 Not Found`  
3. Проверить сообщение об ошибке  

**Запрос:**  
[GET /api/1/{sellerID}/itemID] (https://qa-internship.avito.com/api/1/statistic/271a2549-611b-4ca4-abf3-eb0bc8838837)

**Ожидаемый результат:**  
- Сервер возвращает `404 Not Found`, сообщение "Item not found"  

```json
{
    "result": {
        "message": "statistic 271a2549-611b-4ca4-abf3-eb0bc8838837 not found",
        "messages": null
    },
    "status": "404"
}
```
---

## 5. Негативные тесты (общие)

#### ТК-09 Отправка запроса на несуществующий маршрут 
**Шаги:**  
1. Отправить `GET` запрос без авторизационного токена  
2. Проверить, что статус ответа `404Not Found`
3. Проверить сообщение об ошибке

**Запрос:**
GET https://qa-internship.avito.com/api/1/ads 

**Ожидаемый результат:**  
- Сервер возвращает `404Not Found`

```json
{
    "message": "route /api/1/ads not found",
    "code": 404
}
```
---

#### ТК-10 Отправка запроса POST без тела запроса
**Шаги:**  
1. Отправить `POST` запрос c пустым телом запроса 
2. Проверить, что статус ответа `400 Bad Request`  
3. Проверить сообщение об ошибке

**Запрос:**
POST https://qa-internship.avito.com/api/1/item

**Ожидаемый результат:**  
- Сервер возвращает `400 Bad Request`, сообщение "Invalid sellerID format"  

```json
{
    "message": "invalid content type",
    "code": 400
}
```

---

## 6. Повторное создание объявления (`POST /api/1/item`)

### ТК-11 Попытка создания объявления с уже существующим sellerID

**Шаги:**
1. Отправить `POST` запрос с повторяющимся `sellerID`, который уже существует в системе.
2. Проверить, что статус ответа `400 Bad Request`.
3. Проверить сообщение об ошибке, например, "SellerID already exists".

**Тело запроса:**

```json
{
  "sellerID": 112277,
  "name": "Another Example Item",
  "price": 1500,
  "description": "Another description of the item"
}
```

**Ожидаемый результат:**  
Сервер возвращает ошибку с кодом 400 Bad Request и сообщение "SellerID already exists". 

```json
{
    "message": "SellerID already exists",
    "code": 400
}
```
---

## 7. Валидация данных объявления (`POST /api/1/item`)

### ТК-12 Отправка запроса с некорректным значением price

**Шаги:**
1. Отправить `POST` запрос с некорректным значением `price`, например, отрицательным числом или нулем.
2. Проверить, что статус ответа `400 Bad Request`.
3. Проверить сообщение об ошибке, например, "Invalid price value".

**Тело запроса:**
```json
{
  "sellerID": 112277,
  "name": "Item with invalid price",
  "price": -500,
  "description": "This item has an invalid price"
}
```

**Ожидаемый результат:**  

Сервер возвращает ошибку с кодом 400 Bad Request и сообщение "Invalid price value".

```json
{
    "message": "Invalid price value",
    "code": 400
}
```
---




